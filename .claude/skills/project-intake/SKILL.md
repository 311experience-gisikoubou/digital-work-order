---
name: project-intake
description: Use when a user starts a new project or meaningful feature from a short request such as これがしたい, wants a one-page project brief before implementation, or needs a LIGHT/FULL intake decision before entering the existing Foundation workflow.
---

# Project Intake (Native Skill Wrapper)

This file exists only so Claude Code's native skill loader can auto-detect the shared skill. It is not the source of truth.

## Instruction

1. First, read the canonical skill file:
   `../../../.agents/skills/project-intake/SKILL.md`
2. Follow that file completely as the single source of truth.
3. If this wrapper conflicts with the canonical file, the canonical file wins.

Do not duplicate the canonical skill body here.
